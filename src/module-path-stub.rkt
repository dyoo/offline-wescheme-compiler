#lang planet dyoo/whalesong

(provide module-path-index-join)


;; Since we don't have a real module path resolver offline, we need to
;; do something special here...

(define (module-path-index-join path mpi)
  'fixme)

